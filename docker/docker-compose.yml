version: "3"

services:
    kafka:
        hostname: kafka
        container_name: kafka
        build:
            context: .
            dockerfile: Dockerfile-kfk
        ports:
            - "9092:9092"
    app:
        hostname: app
        container_name: app
        build:
            context: .
            dockerfile: Dockerfile
        depends_on:
            - kafka